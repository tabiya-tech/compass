// REVIEW: I think this is a common type, also used in the Briefing, we can move the type outside at the root of the feature. and re-use it
export enum EffortType {
  TIME_BASED = "time_based",
  WORK_BASED = "work_based",
}