describe("render tests", () => {
  test
    // .each([
    // REACTIONS only
    // TIMESTAMP only
    // REACTIONS and TIMESTAMP
    // ])
    ("should render the visible components correctly", () => {
    // GIVEN some props
    // WHEN the component is rendered
    // THEN expect the container to be in the document
    // AND expect the correct components to be rendered
    // AND expect the component to match snapshot
  });
});
