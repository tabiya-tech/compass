name: Admin Frontend CI & Artifact Upload

on:
  workflow_call:
    inputs:
      upload-artifacts:
        required: true
        type: boolean
        description: 'Whether to upload deployable artifacts'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Use Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'yarn'
          cache-dependency-path: admin-frontend/yarn.lock

      - name: Install node packages
        run: yarn install
        working-directory: admin-frontend

      - name: Check Format
        run: yarn run format:check
        working-directory: admin-frontend

      - name: Lint Admin Frontend
        run: yarn run lint
        working-directory: admin-frontend

      - name: Compile
        run: yarn run compile
        working-directory: admin-frontend

      - name: Test Admin Frontend
        run: yarn run test --watchAll=false --passWithNoTests
        working-directory: admin-frontend

  accessibility:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Use Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'yarn'
          cache-dependency-path: admin-frontend/yarn.lock

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-browsers-${{ runner.os }}-v1
          restore-keys: playwright-browsers-${{ runner.os }}-

      - name: Install dependencies
        run: yarn install
        working-directory: admin-frontend

      - name: Accessibility Tests
        run: |
          npx playwright install --with-deps
          yarn run build-storybook && yarn run test:accessibility
        working-directory: admin-frontend

  build-and-upload:
    runs-on: ubuntu-latest
    if: ${{ inputs.upload-artifacts }}
    needs: [ test, accessibility ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Use Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'yarn'
          cache-dependency-path: admin-frontend/yarn.lock

      - name: Install node packages
        run: yarn install
        working-directory: admin-frontend

      - name: Authenticate to google cloud
        id: auth
        uses: google-github-actions/auth@v2.1.2
        with:
          credentials_json:
            ${{ secrets.GCP_LOWER_ENVS_SERVICE_ACCOUNT_JSON }}

      - name: Run build and upload script.
        shell: bash
        run: |
          ./iac/scripts/build-and-upload-fe.sh ${{ vars.ARTIFACT_REGISTRY_REGION }} ${{ secrets.GCP_REALM_ROOT_PROJECT_ID }} $GITHUB_STEP_SUMMARY $GITHUB_RUN_NUMBER admin-frontend
